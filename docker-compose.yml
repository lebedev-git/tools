services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3010:3000"
    restart: always
    env_file:
      - .env
    environment:
      - PORT=3000
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
